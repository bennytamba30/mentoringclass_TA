{{-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Mentee Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: sans-serif;
            background-color: #f1f5f9;
            padding: 2rem;
        }

        .card {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            padding: 2rem;
            max-width: 600px;
            margin: auto;
            text-align: center;
        }

        .card h1 {
            font-size: 1.75rem;
            margin-bottom: 1rem;
        }

        .card p {
            font-size: 1rem;
            color: #4b5563;
        }

        .logout-btn {
            margin-top: 2rem;
            display: inline-block;
            padding: 0.5rem 1rem;
            background-color: #ef4444;
            color: white;
            border: none;
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: bold;
        }

        .logout-btn:hover {
            background-color: #dc2626;
        }
    </style>
</head>

<body>
    <div class="bg-white rounded-xl shadow-lg p-10 text-center max-w-lg w-full">
        <h1 class="text-2xl font-bold mb-4">Selamat datang,!</h1>
        <p class="text-gray-700 mb-6">Ini adalah dashboard utama untuk Mentee dalam sistem LMS Mentoring.</p>

        <form method="POST" action="{{ route('logout') }}">
            @csrf
            <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-6 py-2 rounded">
                Logout
            </button>
        </form>
    </div>
</body>

</html> --}}
